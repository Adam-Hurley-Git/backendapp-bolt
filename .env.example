# ========================================
# Supabase Configuration
# ========================================
# Public variables (accessible in browser)
PUBLIC_SUPABASE_URL=your_supabase_project_url
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Private variables (server-side only)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ========================================
# Paddle Configuration
# ========================================
# Public variables (accessible in browser)
PUBLIC_PADDLE_VENDOR_ID=your_paddle_vendor_id
PUBLIC_PADDLE_ENVIRONMENT=sandbox

# Private variables (server-side only)
PADDLE_VENDOR_ID=your_paddle_vendor_id
PADDLE_VENDOR_AUTH_CODE=your_paddle_vendor_auth_code
PADDLE_PUBLIC_KEY=your_paddle_public_key
PADDLE_WEBHOOK_SECRET=your_paddle_webhook_secret

# ========================================
# Application Configuration
# ========================================
# Private variables (server-side only)
JWT_SECRET=your_jwt_secret_key_here

# ========================================
# Admin Configuration
# ========================================
# Private variables (server-side only)
ADMIN_EMAIL=admin@yourdomain.com

# ========================================
# CLOUDFLARE PAGES DEPLOYMENT NOTES
# ========================================
# When deploying to Cloudflare Pages:
# 1. Set all these environment variables in the Cloudflare Pages dashboard
# 2. Go to: Settings > Environment Variables
# 3. Add variables for both Production and Preview environments
# 4. DO NOT commit .env or .env.local files to git
# 5. Variables prefixed with PUBLIC_ are exposed to the browser
# 6. Variables without PUBLIC_ prefix are server-side only
# 7. No nodejs_compat flag needed - uses Web APIs only